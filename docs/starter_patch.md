# Starter patch contents

This patch adds the first runnable benchmark layer for `guardrail-gym`:

- model adapter interface and deterministic `MockAdapter`
- baseline system abstractions
- six runnable baselines
- benchmark runner and simple evaluation metrics
- starter benchmark YAML with healthcare and finance scenarios
- smoke tests and a baseline runner script

Use this patch as the first real commit after the initial scaffold.
